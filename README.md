# 🛒 NELON-ECOMMERCE

Минималистичное e-commerce приложение на **Java + Spring Boot**.

## 🚀 Технологии

* **Java 17+**
* **Spring Boot (Web, Security, Data JPA)**
* **JWT аутентификация**
* **PostgreSQL**
* **MapStruct**
* **Lombok**

## ✨ Возможности

* Регистрация и авторизация пользователей
* Роли: **Admin / User**
* CRUD для продуктов и категорий
* Добавление изображений
* Корректная обработка ошибок
* Удобные DTO и маппинг через MapStruct

## 📦 Структура проекта (кратко)

```
src/
 ├── controller/     — REST-контроллеры
 ├── services/       — бизнес-логика
 ├── entities/       — JPA сущности
 ├── dto/            — входящие/выходящие DTO
 ├── mappers/        — MapStruct мапперы
 ├── repositories/   — доступ к БД
 └── exception/      — кастомные ошибки и handler
```

## 🛠 Конфигурация

**application.yml**:

* Настройки БД PostgreSQL
* JWT секрет и время жизни токена
* Настройки CORS (при необходимости)

## ▶️ Запуск проекта

```bash
./mvnw spring-boot:run
```

или

```bash
mvn spring-boot:run
```

## 🔑 Авторизация

Используется **JWT**.
После логина необходимо передавать:

```
Authorization: Bearer <token>
```

## 📘 Будущее развитие

* Refresh токены
* Docker
* Оптимизация запросов
* Тестирование (JUnit + Mockito)

